// A JS code to find the no of digits in a number.

const number = 12345;
const numberString = number.toString();
const numberOfDigits = numberString.length;
console.log(`Number of digits in ${number} is: ${numberOfDigits}`);
